# SolenOS production environment — committed (NEXT_PUBLIC_* vars are safe to expose).
# Netlify bakes this into the client bundle at build time.
# All frontend API calls go through the SAME-ORIGIN Netlify proxy (see netlify.toml).
# NEXT_PUBLIC_API_URL intentionally empty so apiUrl() returns relative /api paths.
NEXT_PUBLIC_API_URL=
