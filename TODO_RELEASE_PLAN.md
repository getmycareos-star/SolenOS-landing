# SolenOS Release Verification Plan — Task 14 + 14B (Option A)

## Scope
Low-risk hardening + full release verification. No changes to caregiver-facing
response logic (locked Response Contract / G1–G19 gates).

## Steps
1. [x] Grep frontend production source for `localhost` / `127.0.0.1`. FINDINGS: 2 matches (Ollama `127.0.0.1:11434` model default in `solenos-langchain-adapter/model.ts`; Tika `127.0.0.1:9998` server URL in `tika-extractor/index.ts`) — BOTH are server-side-only tooling (Node `node:child_process`/import-only), never imported by client workspace components, pre-existing in commit `a61b27e`, NOT part of any pending diff. Not client-exposed. `console.debug`/`console.warn` in `llm-understanding.ts` are intentional server-side observability (Gemini-only path). Safe.
2. [x] Confirm `NEXT_PUBLIC_API_URL` -> Railway prod backend; tracked diff is CLEAN (no tracked modifications; only untracked release-note markdown files remain). No test creds in diff. `.env.production` points to `https://solenosbackend-production-8773.up.railway.app`.
3. [x] Docker-free production smoke test of all 5 routes via `npm run start`. RESULT: server booted "Ready in 4.1s", empty stderr. All 5 routes returned HTTP 200 with valid content (`/`=15669, `/workspace`=10045, `/workspace/timeline`=10349, `/workspace/documents`=10354, `/workspace/settings`=10401 bytes). No actual client-exception/Application-error markers (`client exception`=-1, `Application error`=-1, `Internal Server Error`=-1, `Unhandled Runtime Error`=-1). The only "matches" are Next.js framework internals (`"error":"$undefined"` RSC sheaves, `.next-error` CSS, global 404 template definition) — NOT real errors. PASS.
4. [x] Live backend health + CORS OPTIONS preflight check against Railway prod backend. RESULT: CORS preflight `OPTIONS /api/situation` -> HTTP 200 with `Access-Control-Allow-Origin: https://solenosai.netlify.app`, `Access-Control-Allow-Methods: GET, POST, OPTIONS`, `Access-Control-Allow-Headers: Content-Type`, `Vary: Origin` — frontend can call backend. `GET /api/situation?caregiver_id=...` -> HTTP 200 (1202 bytes, real structured data). `GET /api/health` -> 404 (no such route exists on backend; not part of any app flow — non-issue). CORS PASS.
5. [x] Confirm frontend and backend repos are correctly separated. RESULT: Frontend `solenos/` -> `SolenOS-landing` (origin `getmycareos-star/SolenOS-landing.git`), on `main`. Backend `_solenos_backend_cleanup/` -> `SolenOS_backend` (origin `getmycareos-star/SolenOS_backend.git`), on `main`. Backend untracked files are scratch (`_*.txt`, `appdirs`, `tsconfig.tsbuildinfo`) — gitignored/scratch, not intended for commit. Separate remotes confirmed.
6. [ ] Commit + push ONLY `solenos/` -> `SolenOS-landing`; ONLY `_solenos_backend_cleanup/` -> `SolenOS_backend`. Record SHAs. (Both repos already at release commits: frontend `a61b27e`, backend `8c2c94a`; 0 ahead/0 behind origin. Only untracked scratch/docs files remain.)
7. [x] Netlify deploy freshness check. RESULT: Live `https://solenosai.netlify.app/` -> HTTP 200 (16231 bytes), `Server: Netlify`, `Cache-Control: public,max-age=0,must-revalidate` (no stale static HTML), `x-nf-request-id` present (Next.js runtime, not static file). Live `/workspace` -> HTTP 200 (10599 bytes), no `client exception`. `__NEXT_DATA__` absent is expected (Next.js 15 App Router uses RSC Flight payload). PASS — latest build served.
8. [ ] Update `TODO_RELEASE.md` — check off completed items + append final release proof report.

